# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(local_mapperTaskLib)
ADD_LIBRARY(${LOCAL_MAPPER_TASKLIB_NAME} SHARED 
    ${LOCAL_MAPPER_TASKLIB_SOURCES}
    MapGenerator.cpp)
add_dependencies(${LOCAL_MAPPER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${LOCAL_MAPPER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${LOCAL_MAPPER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${LOCAL_MAPPER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LOCAL_MAPPER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LOCAL_MAPPER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LOCAL_MAPPER_TASKLIB_HEADERS}
    DESTINATION include/orocos/local_mapper)

